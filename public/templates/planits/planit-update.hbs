
<div class="main-container">
  <h1>{{title}}</h1>
  <div class="inner-container-column">
    <form class="form planit-form">
    <p id="errorMessage"></p>
      <label for="title">Title:</label>
      <input class="form-control" type="text" name="title" value="{{planit.title}}" />
      <div class="planit-dropdown">
        <button class="ben-will-murder-you-if-remove-this-class-category btn btn-default edit-planit-btn dropdown-toggle" type="button" data-toggle="dropdown">
        {{#if category}}
          {{category}}
        {{else}}
          Category
        {{/if}}
        <span class="caret"></span>
        </button>
        <ul class="dropdown-menu">
          {{#each planit_types}}
            <li onclick="selectPlanitType({{id}});">{{name}}</li>
          {{/each}}
        </ul>
      </div>
      <input class="planit-type hidden" type="text" name="planit_type_id" value="{{planit.planit_type_id}}" />
      <label for="budget">Budget:</label>
      <input class="planit-budget form-control" type="number" min="0" name="budget" onblur="validateBudget(budget)" value="{{formattedCurrency}}" />
      <label for="date">Start Date:</label>
      <input class="form-control" type="date" name="start_date" value="{{startDate}}" />
      <label for="date">End Date:</label>
      <input class="form-control" type="date" name="end_date" value="{{endDate}}" />
      <label for="street_address">Street Address:</label>
      <input class="form-control" type="text" name="street_address" value="{{planit.street_address}}"><br>
      <label for="city">City:</label>
      <input type="text" name="city" value="{{planit.city}}" />

      <input class="form-control" type="text" name="city" value="{{planit.city}}" />
      <div class="planit-dropdown">
        <button class="ben-will-murder-you-if-remove-this-class-state btn btn-default edit-planit-btn dropdown-toggle" type="button" data-toggle="dropdown">
          {{#if planit.state}}
            {{planit.state}}
          {{else}}
            State
          {{/if}}
          <span class="caret"></span>
        </button>
        <ul class="dropdown-menu">
          {{#each states}}
            <li onclick="selectState({{@index}});">{{this}}</li>
          {{/each}}
        </ul>
      </div>
      <input class="planit-state hidden" type="text" name="state" value="{{planit.state}}" />
      <label for="zipcode">Zipcode:</label>
      <input class="form-control" type="text" name="zipcode" id="zipcode"  value="{{planit.zipcode}}">
      <label class="description-margin" for="description">Description:</label>
      <textarea class="form-control" name="description" rows="8" cols="40">{{planit.description}}</textarea>

      {{#if update}}
        <input class="btn btn-custom form-submit-btn" type="submit" value="Update" onclick="updatePlanitPut(event, {{planit.id}});" />
      {{else}}
        <input class="btn btn-custom form-submit-btn createplanit" type="submit" value="Create" onclick="createPlanitPost(event);" />
      {{/if}}
    </form>
  </div>
</div>
